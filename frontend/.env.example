# Production / explicit API base (include protocol, no trailing slash)
# Example: https://your-backend.vercel.app
VITE_API_URL=

# Optional: local dev proxy target (defaults to http://localhost:4000)
# VITE_DEV_PROXY_TARGET=http://localhost:4000
